# Build output
build/
out/
bin/
target/
!gradle/wrapper/gradle-wrapper.jar
.gradle/
!**/src/**/build/
!**/src/**/bin/
.study/

# Node (web/)
node_modules/
.next/
.turbo/

# Terraform (infra/)
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform.lock.hcl

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
!.vscode/settings.json.example
.kotlin/
.classpath
.project
.settings/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Local configuration and anything that could carry a credential.
# The pattern is deliberately wide: a file this repository does not name is not tracked.
.env
.env.*
!.env.example
*.local.yml
*.local.yaml
*.local.properties
application-local.yml
application-local.yaml
application-secret*.yml
secrets/
*.pem
*.p12
*.jks
*.keystore
*-service-account*.json
credentials*.json

# Generated seed data -- see docs/decisions/publication-readiness.md PUB-7.
#
# THIS REPOSITORY TRACKS NO DATA FILE. The seed is produced by the generator in seed/,
# from a fixed seed value, so anyone can reproduce it. A committed dataset would be a
# dataset nobody could prove the provenance of, and the CI guard enforces the same rule
# from the other side so that a rename does not defeat it.
seed-out/
*.csv
*.tsv
*.jsonl
*.parquet
*.dump
*.bak
*.sql.gz

# Measurement output. Regenerated by the run that produced it; the NUMBERS live in
# docs/reports/, which is where a reader looks for them.
load/out/
*.gc.log
*.hprof
*.jfr

# Scratch
*.log
coverage/
.jacoco/
scratch/
tmp/

# Personal study notes. Untracked for two reasons, and the second is the load-bearing one:
#   1. AGENTS.md requires repository documents to be written in English. These are Korean.
#   2. This repository is public, and `git add -A` is used routinely here. An untracked-but
#      -not-ignored directory is one careless stage away from being published.
.study/
